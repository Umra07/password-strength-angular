export enum PasswordStrength {
  Easy = 'easy',
  Medium = 'medium',
  Strong = 'strong',
  InsufficientLength = 'insufficient length',
}

export enum BarsColors {
  Gray = 'gray',
  Green = 'green',
  Yellow = 'yellow',
  Red = 'red',
}
